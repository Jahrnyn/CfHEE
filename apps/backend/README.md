# Backend

Minimal FastAPI application for the first project slice.

## Run

```bash
python -m venv .venv
.venv\Scripts\activate
python -m pip install -e .
python -m uvicorn cfhee_backend.main:app --reload
```
